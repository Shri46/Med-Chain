// Note: We are using react-hot-toast in ToastContext. 
// This file is a placeholder or wrapper if we need custom toast UI in the future.
// For now, the ToastContext handles the actual toast rendering.
import React from 'react';

export const Toast = () => {
    return null;
};
